plugins {
  id "com.moowork.node" version "1.2.0"
}

version '0.0.1'

node {
  version = '8.11.1'
  npmVersion = '6.1.0'
  download = true
}

task build(type: NpmTask) {
  args = ['run', 'build']
}

build.dependsOn(npm_install)
